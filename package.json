{
  "name": "stellar-shelter",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "clean": "rm -rf node_modules apps/*/node_modules"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^13.3.0"
  }
}
